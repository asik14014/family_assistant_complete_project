# Core
ccxt          # биржевой адаптер (Binance, OKX и др.)
fastapi      # API и TradingView вебхук
uvicorn[standard]  # ASGI-сервер

# DB
SQLAlchemy    # ORM
psycopg2-binary  # драйвер Postgres (для SQLite не нужен)
alembic       # миграции (если решишь использовать)

# Utils
requests      # Telegram-алерты
python-dotenv  # .env загрузка
loguru         # удобные логи

# Monitoring
prometheus-client  # /metrics endpoint для Prometheus

# Optional (для экспериментов/дашбордов)
pandas         # анализ данных / backtest
matplotlib     # простые графики, если нужно