networks:
  default:
    external:
      name: aimaplus_default
      
services:

  assistant-sqs-listener:
    build:
      context: .
      dockerfile: Dockerfile
    command: python main_sqs_listener.py
    env_file:
      - .env.docker
    volumes:
      - .:/app